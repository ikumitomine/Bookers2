<% if flash[:notice_e] %>
    <p class="notice"><%= flash[:notice_e] %></p>
<% end %>

<h1>User info</h1>
<% if @user.id == current_user.id %>
	<%= attachment_image_tag @user, :profile_image, :fill, 100, 100, fallback: "no_image.jpg"%>
	<table>
  		<tr align="left"><th>name</th><td><%= current_user.name %></td></tr>
  		<tr align="left"><th>introduction</th><td><%= current_user.introduction %></td></tr>
	</table>
	<%= link_to "edit", edit_user_path(@user.id) %>
<% else %>
	<%= attachment_image_tag @user, :profile_image, :fill, 100, 100, fallback: "no_image.jpg"%>
	<table>
  		<tr align="left"><th>name</th><td><%= @user.name %></td></tr>
  		<tr align="left"><th>introduction</th><td><%= @user.introduction %></td></tr>
	</table>
	<%= link_to "edit", user_path(current_user.id) %>
<% end %>

<h1>Books</h1>
<main>
<table class="table">
   <tr>
   	<th> </th>
　　 <th>Title</th>
　　 <th>option</th>
   </tr>
   <% @books.each do |book| %>
    <tr>
     <td><%= attachment_image_tag @user, :profile_image, :fill, 40, 40, fallback: "no_image.jpg"%></td>
　　  <td><%= link_to "book.title", book_path(book.id) %></td>
	 <td><%= book.body %></td>
    </tr>
   <% end %>
</table>