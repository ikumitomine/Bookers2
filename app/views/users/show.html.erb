<header>
<h1>Books</h1>
</header>

<main>
<table class="table">
   <tr>
   	<th> </th>
　　 <th>Title</th>
　　 <th>option</th>
   </tr>
   <% @books.each do |book| %>
    <tr>
     <td><%= @user.name %></td>
　　  <td><%= link_to "book.title", book_path(book.id) %></td>
	 <td><%= book.body %></td>
    </tr>
   <% end %>
</table>